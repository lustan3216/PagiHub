// / <reference types="cypress" />

context('Actions', () => {
  beforeEach(() => {
    cy.visit('https://example.cypress.io/commands/actions')
  })

  describe('visible', () => {
    it('layersRoot')
  })

  describe('touchable', () => {
    it('layersRoot')
  })
})
