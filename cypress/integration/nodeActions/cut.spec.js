// / <reference types="cypress" />

context('Actions', () => {
  beforeEach(() => {
    cy.visit('https://example.cypress.io/commands/actions')
  })

  describe('component', () => {
    it('layersRoot')
  })
  describe('component set', () => {
    it('layersRoot')
  })
  describe('components', () => {
    it('layersRoot')
  })
  describe('component set', () => {
    it('layersRoot')
  })
})
