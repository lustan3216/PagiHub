// / <reference types="cypress" />

context('Actions', () => {
  beforeEach(() => {
    cy.visit('https://example.cypress.io/commands/actions')
  })

  describe('node', () => {
    it('layersRoot')
  })
  describe('node set', () => {
    it('layersRoot')
  })
  describe('nodes', () => {
    it('layersRoot')
  })
  describe('node set', () => {
    it('layersRoot')
  })
})
