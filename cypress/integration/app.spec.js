// / <reference types="cypress" />

context('Actions', () => {
  beforeEach(() => {
    cy.visit('https://example.cypress.io/commands/actions')
  })

  describe('publish', () => {
    it('layersRoot')
  })
1
  describe('preview', () => {
    it('layersRoot')
  })

  describe('project', () => {
    it('layersRoot')
  })
})
