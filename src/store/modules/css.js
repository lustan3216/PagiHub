import { SET } from '../index'

const state = {
  global: [],
  components: []
}

const mutations = {
  SET
}

const getters = {
}

export default {
  namespaced: true,
  state,
  mutations,
  getters
}
