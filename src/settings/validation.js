export const pxPercent = {
  type: 'string',
  pattern: /(%|px)$/,
  message: '需選擇px或%'
}

export const required = {
  required: true,
  message: '一定要填喔'
}

export const stringify = {
  type: 'string',
  message: '要是文字喔'
}
