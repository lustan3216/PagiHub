const number = {}

const string = {}

const select = {}

const slider = {}

const boolean = {}

const common = {
  disabled: boolean
}

export default {
  maxlength: {
    type: Number,
    default: '',
    required: true
  }
}
